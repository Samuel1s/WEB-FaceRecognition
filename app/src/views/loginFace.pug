extends layout.pug 

block head 
    link(rel='stylesheet', href='/css/authFace.css')
    title= siteTitle

block content 
    section
        article#role-camera 
            video#video(autoplay) 
            canvas#canvas 
        article#role-attr
            h3 Identificando rosto de #{data.firstName}
            div
                button#test.btn.btn-primary Teste

block script 
    script(src='/js/api_find.js')
    script.
        // Cognitive Services variables.
        const API_URL = '#{API_URL}'
        const KEY = '#{KEY}'

        // Local endpoint variable.
        const LOCAL_URL = '#{LOCAL_URL}'

        // General variables.
        const image64 = '#{data.userImage}'


